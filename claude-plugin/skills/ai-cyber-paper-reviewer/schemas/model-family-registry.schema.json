{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://local.invalid/ai-cyber-paper-reviewer/model-family-registry.schema.json",
  "title": "Detached Canonical Model-Family Registry",
  "description": "A curator-controlled registry used outside a review bundle. Entries name model-family roots, not endpoint aliases or release variants. Conformance validates declared names only; it does not attest to the model that actually executed a review.",
  "type": "object",
  "additionalProperties": false,
  "required": ["registry_version", "canonical_families"],
  "properties": {
    "registry_version": {"const": "1.0.0"},
    "canonical_families": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9](?:[a-z0-9.-]*[a-z0-9])?/[a-z0-9](?:[a-z0-9.-]*[a-z0-9])?$"
      }
    }
  }
}
