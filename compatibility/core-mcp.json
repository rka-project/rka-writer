{
  "_comment": "Optional RKA Core integration. Copy this server entry into your local MCP configuration only when you explicitly want Writer to use an installed RKA Core instance.",
  "mcpServers": {
    "rka": {
      "command": "rka",
      "args": [
        "mcp"
      ],
      "env": {
        "RKA_API_URL": "http://127.0.0.1:9712"
      }
    }
  }
}
